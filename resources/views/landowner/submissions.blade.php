@extends('layouts.landowner') @section('page-heading','Submissions & Legal Tracking')
@section('content')<div class="erp-hero compact"><div><span class="erp-kicker">LIVE LEGAL PIPELINE</span><h2>From submission to construction.</h2><p>Every verification and agreement milestone is visible here.</p></div><a class="erp-button" href="{{ route('land.submit') }}">+ New submission</a></div>@forelse($submissions as $submission)@php($stages=['Submitted','Legal Vetting','Lawyer Assigned','JV Agreement Drafted','Active Project']) @php($current=max(0,array_search($submission->stage,$stages)!==false?array_search($submission->stage,$stages):0))<article class="erp-panel land-track-card"><div class="erp-panel-head"><div><span class="erp-kicker">{{ $submission->code }}</span><h3>{{ $submission->title }}</h3><p>{{ $submission->location }} · {{ $submission->katha_size }} Katha</p></div><span class="erp-pill">{{ $submission->status }}</span></div><div class="legal-timeline">@foreach($stages as $index=>$stage)<div class="{{ $index<=$current?'done':'' }}"><b>{{ $index+1 }}</b><span>{{ $stage }}</span></div>@if(!$loop->last)<i class="{{ $index<$current?'done':'' }}"></i>@endif @endforeach</div><div class="land-track-details"><div><span>Assigned lawyer</span><b>{{ $submission->lawyer?->name??'Pending assignment' }}</b><small>{{ $submission->lawyer?->specialization }}</small></div><div><span>JV share</span><b>{{ $submission->agreement?$submission->agreement->landowner_share_pct.'% Landowner / '.$submission->agreement->developer_share_pct.'% Developer':'Awaiting legal approval' }}</b></div><div><span>Agreement</span><b>{{ $submission->agreement?ucwords(str_replace('_',' ',$submission->agreement->status)):'Not drafted' }}</b></div></div>@if($submission->rejection_reason)<div class="erp-error">{{ $submission->rejection_reason }}</div>@endif @if($submission->agreement)<details class="jv-terms"><summary>View JV agreement terms</summary><p>{{ $submission->agreement->terms }}</p></details>@endif</article>@empty<section class="erp-panel erp-empty">No submissions are connected to this account yet.</section>@endforelse @endsection
