<!DOCTYPE html>
<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>@yield('title', 'Investor Portal | Intern Estate')</title>@vite(['resources/css/app.css', 'resources/js/app.js'])</head>
<body class="portal-body investor-portal-body"><div class="portal-layout"><aside class="portal-sidebar investor-sidebar" id="portalSidebar"><a href="{{ route('investor.dashboard') }}" class="portal-brand"><span class="brand-icon">IE</span><span class="brand-name">Intern<span>Estate</span></span></a><p class="sidebar-label">INVESTOR PORTAL</p><nav class="sidebar-navigation"><a href="{{ route('investor.dashboard') }}">Dashboard</a><a href="{{ route('investor.ledger') }}">Payment Ledger</a><a href="{{ route('investor.documents') }}">Document Vault</a><a href="{{ route('investor.notifications') }}">Notifications</a></nav><div class="sidebar-bottom"><a href="{{ route('landing') }}">Public Website</a><form method="POST" action="{{ route('logout') }}">@csrf<button type="submit">Logout</button></form></div></aside><div class="portal-main"><header class="portal-topbar"><button id="sidebarToggle" class="sidebar-toggle" type="button" aria-label="Toggle sidebar">Menu</button><div><p class="topbar-date">{{ now()->format('l, d F Y') }}</p><h1>@yield('page-heading', 'Investor Dashboard')</h1></div><div class="user-menu"><div class="user-avatar">{{ strtoupper(substr(auth()->user()->name, 0, 1)) }}</div><div><strong>{{ auth()->user()->name }}</strong><span>Investor</span></div></div></header><main class="portal-content">@if(session('success'))<div class="portal-alert">{{ session('success') }}</div>@endif @yield('content')</main></div></div>@stack('scripts')</body></html>
